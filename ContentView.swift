import SwiftUI

struct ContentView: View {
  @State var answer = ""
    
    var body: some View {
        VStack {
           
            Text("\(answer)")
            
            Button("multiply"){
                
            }
            
        }
    }
}
